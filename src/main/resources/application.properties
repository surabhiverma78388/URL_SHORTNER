spring.application.name=urlshortner

# Database Configuration - Uses Railway env vars with fallback for local dev
spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/url}
spring.datasource.username=${MYSQL_USERNAME:database}
spring.datasource.password=${MYSQL_PASSWORD:Dcba@2424}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server port - Railway uses PORT env var
server.port=${PORT:8080}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect