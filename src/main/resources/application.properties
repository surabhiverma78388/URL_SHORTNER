spring.application.name=urlshortner

# Database Configuration - Uses Railway env vars with fallback for local dev
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway
spring.datasource.username=root
spring.datasource.password=CmbArIXrFAYjzxlusbzhxjydQdpqKBnW
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Server port - Railway uses PORT env var
server.port=${PORT:8080}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
